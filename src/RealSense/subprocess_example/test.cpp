#include <iostream>

int main() {
    
    std::cout << ... << std::endl; // std::endl = Inserts a newline character into the output sequence os and flushes it as if by calling os.put(os.widen('\n')) followed by os.flush()
    std::cout.flush(); 
    
    /*
    for (int ii = 0; ii < 10; ++ii) {
        int input;
        std::cin >> input;
        std::cout << input * 2 << std::endl;
        std::cout.flush(); */
    }
}